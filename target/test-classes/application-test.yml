spring:
  data:
    redis:
      host: localhost     # Или имя сервиса из Docker Compose (например, redis)
      port: 6379
      password: redispass     # Если требуется пароль
  session:
    redis:

    # Явно указываем, что сессии хранятся в Redis